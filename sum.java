public class Sum {

    public static void main(String[] args) {

        int a = 5;
        int b = 10;

        int sum = a + b;

        System.out.println("The sum of " + a + " and " + b + " is: " + sum);
    }
}
# This program calculates the sum of two integers, `a` and `b`, and prints the result to the console.
# The variables `a` and `b` are initialized with the values 5 and 10, respectively. The sum is calculated by adding `a` and `b`, and the result is stored in the variable `sum`. Finally, the program prints the sum to the console in a formatted string.